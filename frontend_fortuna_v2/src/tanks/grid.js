
// start at beginning of wall and fill until when?

export function dirtGrid(grid) {
  for (let i = 0; i < x; i++)
  {
    for (let j = 0; j <y; j++)
    { 
       if (x == 10 && y == 0 && grid[x][y] != -1)
       {
          grid.fill(-1, )
          continue;
       }
       else if (x == 60 && y == 0)
    }
  }
}

export function hexGrid(grid) {

}

export function lunarGrid(grid) {

}

export function candenGrid(grid) {
  
}