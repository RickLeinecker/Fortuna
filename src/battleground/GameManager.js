//@flow strict

/*
import typeof Seg from '../geometry/Seg.js';
import typeof Tank from k

// a general purpose class that allows for easy creation
// and deletion of game objects. This helps reduce JS
// cycles so things don't need to import Battleground,
// but can safely call the helper methods anyway
class GameManager {
	getCollisionSegs(): Array<Seg> {
	}

	getTanks(): Array<Tank> {
	}

	createGameObject(toCreate: GameObject): void {
	}

	deleteGameObject(toDelete: GameObject): void {
	}
}

export default GameManager;
*/
